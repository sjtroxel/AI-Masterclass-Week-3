# strawberry-star-server environment variables
# Copy to .env and fill in real values — never commit .env

# Server
PORT=3000

# CORS — comma-separated list of allowed origins
# ALLOWED_ORIGIN=http://localhost:5173

# Future: Supabase service role key (server-side only, never exposed to frontend)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Future: Database connection (if switching from Supabase to direct Postgres)
# DATABASE_URL=postgresql://user:password@localhost:5432/strawberry
